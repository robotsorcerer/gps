# -----------------------------------------------------------------------
# Production runtime dependencies (pinned for reproducibility)
# -----------------------------------------------------------------------
numpy>=1.24,<2.0
scipy>=1.10,<2.0
protobuf>=4.23,<5.0
scikit-image>=0.21,<1.0

# PyTorch CPU-only for CI (GPU variant installed separately in GPU jobs)
torch>=2.1,<3.0

# TensorFlow is legacy; pin to 2.x compat shim for old TF tests
# tensorflow>=2.13,<3.0   # uncomment only in TF test jobs

# -----------------------------------------------------------------------
# Development / test dependencies
# -----------------------------------------------------------------------
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0
pytest-timeout>=2.1,<3.0
pytest-xdist>=3.3,<4.0       # parallel test execution
pytest-mock>=3.11,<4.0

# Memory / profiling helpers used in soak tests
psutil>=5.9,<7.0

# -----------------------------------------------------------------------
# Static analysis / linting (run in CI lint job)
# -----------------------------------------------------------------------
ruff>=0.1,<1.0
mypy>=1.5,<2.0
